# PICO - Personalized Interactive Cryptographic Operations
#
#   python -m venv .venv
#   .venv\Scripts\activate          (Windows)
#   source .venv/bin/activate       (macOS / Linux)
#   pip install -r requirements.txt
#
# Then:
#   python -m pico.cli demo         the Alice and Bob walkthrough
#   python run.py                   the web console on http://127.0.0.1:8000

# --- cryptography -----------------------------------------------------------
# All production-grade primitives: AES-GCM/CBC, RSA-OAEP/PSS, PBKDF2, scrypt,
# HKDF and Diffie-Hellman. PICO never implements these itself.
cryptography>=42.0,<46

# --- web console ------------------------------------------------------------
fastapi>=0.110,<1.0
uvicorn>=0.27,<1.0
pydantic>=2.5,<3.0

# --- tests ------------------------------------------------------------------
# Only needed to run `pytest`; the application itself does not import these.
pytest>=8.0
httpx>=0.27          # FastAPI's TestClient transport
